var func = require("./index")
var context = require("./context")


func(context, {
    body: {
        action: "pages",
        query: "christianiabpos.sharepoint.com|/sites/intranets-location"
    }
});